# Correção de continuidade temporal

Antes de importar o workflow, crie na Data Table `chat_query_state` a coluna String/Text `recent_periods`.

Depois importe `n8n/workflows/gentileza.json` e publique.
